-if resource.any?
  %table
    %thead
      %tr
        %th Оценка приема
        %th Отзыв
        %th Дата публикации
    %tbody
      - resource.each do |report|
        %tr
          %td= report.note
          %td= report.comment
          %td= report.created_at.localtime.strftime("%D")
-else
  %p У вас нет отзывов
